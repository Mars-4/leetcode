class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int ans = 0;
        int mInPrice = prices[0];
        for(int i : prices)
        {
            ans = max(i - mInPrice, ans);
            mInPrice = min(mInPrice, i);
        }
        return ans;
    }
};
